@{
    ViewData["Title"] = "Observation Assigned";
    Layout = "_Layout";
}

<script type="text/javascript">
    $(document).ready(function () {
        $('#viewMemo_memo111').richText({
            imageUpload: false,
            fileUpload: false,
            videoEmbed: false,
            urls: false
        });
        $('#viewMemo_reply').richText({
            imageUpload: false,
            fileUpload: false,
            videoEmbed: false,
            urls: false
        });
    });
    function showMemo(memotxt) {
        $('#viewMemoModel').modal('show');
        $('#viewMemo_memo').html(memotxt);
        //$('#viewMemo_memoTxt').val($('#viewMemo_memo').text());

    }
</script>
<div class="row col-md-12 mt-3">

    <div class="row col-md-12 mt-3">
        <h5 style="color: #45c545;">Assigned Observations</h5>
    </div>
    <div class="row col-md-12 mt-3">
        <table class="table table-bordered mb-0 mt-3 bg-white table-striped">
            <thead>
                <tr>
                    <th class="col-md-1">Sr. No</th>
                    <th class="col-md-5">Entity</th>
                    <th class="col-md-2">Audit Start Date</th>
                    <th class="col-md-2">Audit End Date</th>
                    <th class="col-md-1">Status</th>
                    <th class="col-md-1"></th>
                </tr>
            </thead>
            <tbody>
                @{
                    if (ViewData["AssignedObservations"] != null)
                    {

                        var index = 1;
                        foreach (var item in (dynamic)(ViewData["AssignedObservations"]))
                        {
                            string sdate = @item.AUDIT_STARTDATE.ToString().Split(" ")[0];
                            string edate = @item.AUDIT_ENDDATE.ToString().Split(" ")[0];
                            <tr>
                                <td class="text-center">@index</td>
                                <td>@item.ENTITY_NAME</td>
                                <td>@sdate</td>
                                <td>@edate</td>
                                <td>@item.STATUS</td>
                                <td class="text-center"><a onclick="showMemo('@item.OBSERVATION_TEXT');" data="@item.OBSERVATION_TEXT" class="text-hover font-weight-bold text-success">View</a></td>
                            </tr>
                            index++;
                        }
                    }
                }
            </tbody>
        </table>
    </div>
</div>


<div id="viewMemoModel" class="modal" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title">Observation</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form>
                    <div class="form-group">
                        <label for="viewMemo_memo">Memo</label>
                        <div id="viewMemo_memo" disabled="disabled" style="height:150px;" class="form-control"></div>
                    </div>
                   
                    <div class="form-group">
                        <label for="viewMemo_reply">Reply</label>
                        <textarea id="viewMemo_reply" style="height:150px;" class="form-control"></textarea>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button onclick="saveMemoContent();" type="button" data-dismiss="modal" class="btn btn-danger">Save Memo</button>
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>

